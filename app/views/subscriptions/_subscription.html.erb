<span class="subscription">
  <div class=" buffer-both d-flex justify-content-between align-items-center">
  <% if subscription.user.present? %>
    <%= render partial: 'users/user', object: subscription.user %>
  <% else %>
    <em><%= subscription.user_name %></em>
  <% end %>
    <% if current_user_can_edit?(subscription) %>
    <%= button_to '',
                  event_subscription_path(subscription.event, subscription),
                  method: :delete,
                  form: { data: { turbo_confirm: t('subscriptions.subscription.destroy_confirm') } },
                  class: 'btn-close' %>
  <% end %>
</div>
</span>
