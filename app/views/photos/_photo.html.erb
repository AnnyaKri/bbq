<div class="col-md-3 ">
  <figure class="figure">
    <%= link_to image_tag(photo.photo.variant(:thumb), class: "img-fluid rounded"), photo.photo %>
    <figcaption class="figure-caption">
      Добавил <%= render partial: 'users/user', object: photo.user %>
    </figcaption>
  </figure>
</div>
